MAke a way for the cleint to quit and the server to understnad it. 

For messages: They start with the size OF THE FOLLOWING Message
example:  "5hello"

Avachi is ur mdns resolver

Notes: 

we are still working on the client message system. In the main thread of the clinet the 
thread gets stuck on something and even tho quit is understood it doesnt close

Also a thread is not being close/ gets stuck in the server meaning htat it never closes down
